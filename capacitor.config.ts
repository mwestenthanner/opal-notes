import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pancakesandpixels.opalnotes',
  appName: 'opal-notes',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
